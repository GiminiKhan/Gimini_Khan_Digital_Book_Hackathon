# Quickstart

## Running the development server

1.  Install dependencies:
    ```bash
    npm install
    ```
2.  Start the development server:
    ```bash
    npm start
    ```
This will open a browser window with the local version of the website.
